<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fenda.encyclopedia">

    <application
        android:name="debug.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.fenda.encyclopedia.view.EncyclopediaNotAnswerActivity"></activity>
        <activity android:name="com.fenda.encyclopedia.view.EncyclopediaQuestiionActivity" android:launchMode="singleTask"/>
        <activity android:name="com.fenda.encyclopedia.view.TextActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.fenda.encyclopedia.view.EncyclopediaSharesActivity" />
    </application>

</manifest>